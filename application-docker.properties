# =======================================================
# GLOBAL DOCKER PROFILE - SINGLE POSTGRES INSTANCE
# =======================================================

spring.datasource.url=jdbc:postgresql://postgres-db:5432/vitacare_db

spring.datasource.username=vitacare_user
spring.datasource.password=vitacare_password
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update

eureka.client.service-url.defaultZone=http://service-registry:8761/eureka

# Logging
logging.level.org.vitacare=DEBUG

spring.flyway.create-schemas=true